apiVersion: v1
kind: Service
metadata:
  name: react-cicd-service
  labels:
    app: react-cicd-app
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: react-cicd-app
